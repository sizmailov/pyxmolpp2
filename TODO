- indexation support
   operator []
   optional<ResidueRef> ResidueRef.next()
   optional<ResidueRef> ResidueRef.prev()

- increase test coverage

- organize headers for convenient includes by users
- xtc format
- pseudo trajectory adapter
- trajectory from layout

- update bindings to v1
- support erasure of elements
- prmtop topology
- rework predicates?
